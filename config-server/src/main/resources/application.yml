server:
  port: 8888

spring:  cloud:    config:      server:        git:          uri: https://github.com/Romanope/microservice-config-repo          search-paths:  fornecedor, transportador, loja, routes, gateway